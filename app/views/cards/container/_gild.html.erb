<% if card.golden? %>
  <%= button_to card_goldness_path(card), method: :delete, class: "btn btn--reversed", title: "Demote to normal" do %>
    <%= icon_tag "golden-ticket" %>
    <span class="for-screen-reader">Demote to normal</span>
  <% end %>
<% else %>
  <%= button_to card_goldness_path(card), class: "btn", title: "Promote to golden" do %>
    <%= icon_tag "golden-ticket" %>
    <span class="for-screen-reader">Promote to golden</span>
  <% end %>
<% end %>
